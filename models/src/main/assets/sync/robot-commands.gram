#JSGF V1.0;

grammar robot;

<forward>  = run forward;
<backward>  = run backward;
<stop> = stop;
<max>  = (<forward> | <backward> ) with maximum speed;
<destroy> = destroy yourself;

public <item> = <forward> | <stop> | <backward> | <max> | <destroy>;

